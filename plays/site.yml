- name: MySql
  hosts: bdd
  remote_user: mel
  roles: 
    - role: 'mysql'

- name: Ftp
  hosts: bdd
  remote_user: mel
  roles: 
    - role: 'ftp'

- name: Apache / Php
  hosts: web
  remote_user: mel
  roles: 
    - role: 'apachephp'

- name: Wordpress
  hosts: web
  remote_user: mel
  roles: 
    - role: 'wordpress'